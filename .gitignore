node_modules
dist
scratch
.claude
*.local
.DS_Store

# Intermediarios da geracao do GIF, nunca commitados: o video bruto do
# Playwright e a paleta que o ffmpeg gera entre as duas passagens.
*.webm
paleta.png
